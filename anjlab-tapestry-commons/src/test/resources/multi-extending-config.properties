# In case of multiple extensions they will first extend each other in the specified order
# creating an intermediate base config, resulted intermediate config will then be extended
# with properties from current file
extend.1=extending-config.properties
extend.2=base-config.properties